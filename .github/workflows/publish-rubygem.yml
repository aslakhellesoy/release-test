name: Publish Ruby gem

on:
  push:
    branches:
      - "releases/*"
    tags:
      - "v*"

jobs:
  publish-rubygem:
    runs-on: ubuntu-latest
    steps:
      - name: Publish Ruby gem
        run: echo "I could have published a Ruby gem here"
