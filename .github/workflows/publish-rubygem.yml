name: Publish Ruby gem

on:
  push:
    branches:
      - "!*"
    tags:
      - "v*"

jobs:
  publish-rubygem:
    runs-on: ubuntu-latest
    steps:
      - name: Publish Ruby gem
        env:
          SECRET: ${{ secrets.GITHUB_TOKEN }}
        run: echo "The TOP_SECRET is: ${SECRET}"
